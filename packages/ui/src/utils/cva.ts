import { cva as _cva } from 'class-variance-authority'

/**
 * Re-export cva from class-variance-authority
 * Used for creating component variants with TypeScript support
 */
export const cva = _cva