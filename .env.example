# Database
DATABASE_URL="mysql://dev:dev123@127.0.0.1:3306/gladpros"
SHADOW_DATABASE_URL="mysql://root:root@127.0.0.1:3306/gladpros"

# JWT secret (>=32 chars)
JWT_SECRET="your-very-long-jwt-secret-at-least-32-chars"

# AES-256-GCM key for document encryption (32 bytes base64)
CLIENT_DOC_ENCRYPTION_KEY_BASE64="base64-32-bytes-example=="

# AES-256-GCM key for Worker sensitive data (routing, account, taxId)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY="your-64-char-hex-key-here-replace-with-generated-key-please"

# Next.js
NEXT_TELEMETRY_DISABLED=1
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Performance: variáveis críticas — adicionar ao .env de produção após deploy
TOKEN_VERSION_COLUMN_EXISTS=1
RBAC_TRUST_JWT=1

# Optional: Redis (rate-limits distribuídos)
# Se Redis NÃO estiver disponível, use REDIS_DISABLED=true para evitar timeout de 1s no boot.
# Se Redis estiver disponível, configure REDIS_URL e remova REDIS_DISABLED.
REDIS_DISABLED=true
# REDIS_URL="redis://localhost:6379"

# SMTP (Hostinger example)
SMTP_HOST="smtp.hostinger.com"
SMTP_PORT="465"
SMTP_USER="you@yourdomain.com"
SMTP_PASS="your-smtp-password"
SMTP_FROM="GladPros <you@yourdomain.com>"
SMTP_SECURE="true"

# Seed admin (optional)
SEED_ADMIN_EMAIL=admin@gladpros.local
SEED_ADMIN_PASS=Admin@12345

# Local dev URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
